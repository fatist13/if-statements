// Input
let providedNumber = prompt("Please enter an integer");

// Check userName
if (providedNumber > 0) {
  alert("Positive");
} else if (providedNumber < 0) {
  alert("Negative");
} else {
  alert("zero");
}
